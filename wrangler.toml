name = "renoxf"
compatibility_date = "2026-08-30"

# Static site via Workers Assets - hanya folder public biar node_modules gak keikut (limit 25MiB)
assets = { directory = "./public", not_found_handling = "single-page-application" }
